// API Configuration
const API_CONFIG = {
  // Replace this with your new Vercel backend URL after deployment
  BASE_URL: "https://shema-backend.vercel.app",
  // For local development, uncomment the line below
  // BASE_URL: "http://localhost:5000",
  
  // API endpoints
  ENDPOINTS: {
    ITEMS: "/api/items",
    PAYMENT: "/api/payment"
  }
};

export default API_CONFIG;
